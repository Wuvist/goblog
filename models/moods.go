// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Mood is an object representing the database table.
type Mood struct {
	ID           int    `boil:"id" json:"id" toml:"id" yaml:"id"`
	Mood         string `boil:"mood" json:"mood" toml:"mood" yaml:"mood"`
	BloggerCount int    `boil:"blogger_count" json:"blogger_count" toml:"blogger_count" yaml:"blogger_count"`

	R *moodR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L moodL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MoodColumns = struct {
	ID           string
	Mood         string
	BloggerCount string
}{
	ID:           "id",
	Mood:         "mood",
	BloggerCount: "blogger_count",
}

// moodR is where relationships are stored.
type moodR struct {
}

// moodL is where Load methods for each relationship are stored.
type moodL struct{}

var (
	moodColumns               = []string{"id", "mood", "blogger_count"}
	moodColumnsWithoutDefault = []string{"id", "mood"}
	moodColumnsWithDefault    = []string{"blogger_count"}
	moodPrimaryKeyColumns     = []string{"id"}
)

type (
	// MoodSlice is an alias for a slice of pointers to Mood.
	// This should generally be used opposed to []Mood.
	MoodSlice []*Mood
	// MoodHook is the signature for custom Mood hook methods
	MoodHook func(boil.Executor, *Mood) error

	moodQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	moodType                 = reflect.TypeOf(&Mood{})
	moodMapping              = queries.MakeStructMapping(moodType)
	moodPrimaryKeyMapping, _ = queries.BindMapping(moodType, moodMapping, moodPrimaryKeyColumns)
	moodInsertCacheMut       sync.RWMutex
	moodInsertCache          = make(map[string]insertCache)
	moodUpdateCacheMut       sync.RWMutex
	moodUpdateCache          = make(map[string]updateCache)
	moodUpsertCacheMut       sync.RWMutex
	moodUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var moodBeforeInsertHooks []MoodHook
var moodBeforeUpdateHooks []MoodHook
var moodBeforeDeleteHooks []MoodHook
var moodBeforeUpsertHooks []MoodHook

var moodAfterInsertHooks []MoodHook
var moodAfterSelectHooks []MoodHook
var moodAfterUpdateHooks []MoodHook
var moodAfterDeleteHooks []MoodHook
var moodAfterUpsertHooks []MoodHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Mood) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range moodBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Mood) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range moodBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Mood) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range moodBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Mood) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range moodBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Mood) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range moodAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Mood) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range moodAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Mood) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range moodAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Mood) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range moodAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Mood) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range moodAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMoodHook registers your hook function for all future operations.
func AddMoodHook(hookPoint boil.HookPoint, moodHook MoodHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		moodBeforeInsertHooks = append(moodBeforeInsertHooks, moodHook)
	case boil.BeforeUpdateHook:
		moodBeforeUpdateHooks = append(moodBeforeUpdateHooks, moodHook)
	case boil.BeforeDeleteHook:
		moodBeforeDeleteHooks = append(moodBeforeDeleteHooks, moodHook)
	case boil.BeforeUpsertHook:
		moodBeforeUpsertHooks = append(moodBeforeUpsertHooks, moodHook)
	case boil.AfterInsertHook:
		moodAfterInsertHooks = append(moodAfterInsertHooks, moodHook)
	case boil.AfterSelectHook:
		moodAfterSelectHooks = append(moodAfterSelectHooks, moodHook)
	case boil.AfterUpdateHook:
		moodAfterUpdateHooks = append(moodAfterUpdateHooks, moodHook)
	case boil.AfterDeleteHook:
		moodAfterDeleteHooks = append(moodAfterDeleteHooks, moodHook)
	case boil.AfterUpsertHook:
		moodAfterUpsertHooks = append(moodAfterUpsertHooks, moodHook)
	}
}

// OneP returns a single mood record from the query, and panics on error.
func (q moodQuery) OneP() *Mood {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single mood record from the query.
func (q moodQuery) One() (*Mood, error) {
	o := &Mood{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for moods")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all Mood records from the query, and panics on error.
func (q moodQuery) AllP() MoodSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all Mood records from the query.
func (q moodQuery) All() (MoodSlice, error) {
	var o []*Mood

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Mood slice")
	}

	if len(moodAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all Mood records in the query, and panics on error.
func (q moodQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all Mood records in the query.
func (q moodQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count moods rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q moodQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q moodQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if moods exists")
	}

	return count > 0, nil
}

// MoodsG retrieves all records.
func MoodsG(mods ...qm.QueryMod) moodQuery {
	return Moods(boil.GetDB(), mods...)
}

// Moods retrieves all the records using an executor.
func Moods(exec boil.Executor, mods ...qm.QueryMod) moodQuery {
	mods = append(mods, qm.From("`moods`"))
	return moodQuery{NewQuery(exec, mods...)}
}

// FindMoodG retrieves a single record by ID.
func FindMoodG(id int, selectCols ...string) (*Mood, error) {
	return FindMood(boil.GetDB(), id, selectCols...)
}

// FindMoodGP retrieves a single record by ID, and panics on error.
func FindMoodGP(id int, selectCols ...string) *Mood {
	retobj, err := FindMood(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindMood retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMood(exec boil.Executor, id int, selectCols ...string) (*Mood, error) {
	moodObj := &Mood{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `moods` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(moodObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from moods")
	}

	return moodObj, nil
}

// FindMoodP retrieves a single record by ID with an executor, and panics on error.
func FindMoodP(exec boil.Executor, id int, selectCols ...string) *Mood {
	retobj, err := FindMood(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Mood) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *Mood) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *Mood) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *Mood) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no moods provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(moodColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	moodInsertCacheMut.RLock()
	cache, cached := moodInsertCache[key]
	moodInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			moodColumns,
			moodColumnsWithDefault,
			moodColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(moodType, moodMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(moodType, moodMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `moods` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `moods` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `moods` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, moodPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.Exec(cache.query, vals...)
	if err != nil {
		return errors.Wrap(err, "models: unable to insert into moods")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for moods")
	}

CacheNoHooks:
	if !cached {
		moodInsertCacheMut.Lock()
		moodInsertCache[key] = cache
		moodInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single Mood record. See Update for
// whitelist behavior description.
func (o *Mood) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single Mood record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *Mood) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the Mood, and panics on error.
// See Update for whitelist behavior description.
func (o *Mood) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the Mood.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *Mood) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	moodUpdateCacheMut.RLock()
	cache, cached := moodUpdateCache[key]
	moodUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			moodColumns,
			moodPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update moods, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `moods` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, moodPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(moodType, moodMapping, append(wl, moodPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update moods row")
	}

	if !cached {
		moodUpdateCacheMut.Lock()
		moodUpdateCache[key] = cache
		moodUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q moodQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q moodQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for moods")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o MoodSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o MoodSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o MoodSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MoodSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `moods` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, moodPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in mood slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Mood) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *Mood) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *Mood) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *Mood) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no moods provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(moodColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	moodUpsertCacheMut.RLock()
	cache, cached := moodUpsertCache[key]
	moodUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			moodColumns,
			moodColumnsWithDefault,
			moodColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			moodColumns,
			moodPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert moods, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "moods", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `moods` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(moodType, moodMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(moodType, moodMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	_, err = exec.Exec(cache.query, vals...)
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for moods")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for moods")
	}

CacheNoHooks:
	if !cached {
		moodUpsertCacheMut.Lock()
		moodUpsertCache[key] = cache
		moodUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single Mood record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *Mood) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single Mood record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Mood) DeleteG() error {
	if o == nil {
		return errors.New("models: no Mood provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single Mood record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *Mood) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single Mood record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Mood) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no Mood provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), moodPrimaryKeyMapping)
	sql := "DELETE FROM `moods` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from moods")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q moodQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q moodQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no moodQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from moods")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o MoodSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o MoodSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no Mood slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o MoodSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MoodSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no Mood slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(moodBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `moods` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, moodPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from mood slice")
	}

	if len(moodAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *Mood) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *Mood) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Mood) ReloadG() error {
	if o == nil {
		return errors.New("models: no Mood provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Mood) Reload(exec boil.Executor) error {
	ret, err := FindMood(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *MoodSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *MoodSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MoodSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty MoodSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MoodSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	moods := MoodSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `moods`.* FROM `moods` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, moodPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&moods)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MoodSlice")
	}

	*o = moods

	return nil
}

// MoodExists checks if the Mood row exists.
func MoodExists(exec boil.Executor, id int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `moods` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if moods exists")
	}

	return exists, nil
}

// MoodExistsG checks if the Mood row exists.
func MoodExistsG(id int) (bool, error) {
	return MoodExists(boil.GetDB(), id)
}

// MoodExistsGP checks if the Mood row exists. Panics on error.
func MoodExistsGP(id int) bool {
	e, err := MoodExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// MoodExistsP checks if the Mood row exists. Panics on error.
func MoodExistsP(exec boil.Executor, id int) bool {
	e, err := MoodExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
