// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
	"gopkg.in/volatiletech/null.v6"
)

// BloggerMood is an object representing the database table.
type BloggerMood struct {
	ID        int         `boil:"id" json:"id" toml:"id" yaml:"id"`
	BloggerID int         `boil:"blogger_id" json:"blogger_id" toml:"blogger_id" yaml:"blogger_id"`
	MoodID    int         `boil:"mood_id" json:"mood_id" toml:"mood_id" yaml:"mood_id"`
	MSG       null.String `boil:"msg" json:"msg,omitempty" toml:"msg" yaml:"msg,omitempty"`
	CreateDT  time.Time   `boil:"create_dt" json:"create_dt" toml:"create_dt" yaml:"create_dt"`

	R *bloggerMoodR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L bloggerMoodL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BloggerMoodColumns = struct {
	ID        string
	BloggerID string
	MoodID    string
	MSG       string
	CreateDT  string
}{
	ID:        "id",
	BloggerID: "blogger_id",
	MoodID:    "mood_id",
	MSG:       "msg",
	CreateDT:  "create_dt",
}

// bloggerMoodR is where relationships are stored.
type bloggerMoodR struct {
}

// bloggerMoodL is where Load methods for each relationship are stored.
type bloggerMoodL struct{}

var (
	bloggerMoodColumns               = []string{"id", "blogger_id", "mood_id", "msg", "create_dt"}
	bloggerMoodColumnsWithoutDefault = []string{"blogger_id", "mood_id", "msg"}
	bloggerMoodColumnsWithDefault    = []string{"id", "create_dt"}
	bloggerMoodPrimaryKeyColumns     = []string{"id"}
)

type (
	// BloggerMoodSlice is an alias for a slice of pointers to BloggerMood.
	// This should generally be used opposed to []BloggerMood.
	BloggerMoodSlice []*BloggerMood
	// BloggerMoodHook is the signature for custom BloggerMood hook methods
	BloggerMoodHook func(boil.Executor, *BloggerMood) error

	bloggerMoodQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	bloggerMoodType                 = reflect.TypeOf(&BloggerMood{})
	bloggerMoodMapping              = queries.MakeStructMapping(bloggerMoodType)
	bloggerMoodPrimaryKeyMapping, _ = queries.BindMapping(bloggerMoodType, bloggerMoodMapping, bloggerMoodPrimaryKeyColumns)
	bloggerMoodInsertCacheMut       sync.RWMutex
	bloggerMoodInsertCache          = make(map[string]insertCache)
	bloggerMoodUpdateCacheMut       sync.RWMutex
	bloggerMoodUpdateCache          = make(map[string]updateCache)
	bloggerMoodUpsertCacheMut       sync.RWMutex
	bloggerMoodUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var bloggerMoodBeforeInsertHooks []BloggerMoodHook
var bloggerMoodBeforeUpdateHooks []BloggerMoodHook
var bloggerMoodBeforeDeleteHooks []BloggerMoodHook
var bloggerMoodBeforeUpsertHooks []BloggerMoodHook

var bloggerMoodAfterInsertHooks []BloggerMoodHook
var bloggerMoodAfterSelectHooks []BloggerMoodHook
var bloggerMoodAfterUpdateHooks []BloggerMoodHook
var bloggerMoodAfterDeleteHooks []BloggerMoodHook
var bloggerMoodAfterUpsertHooks []BloggerMoodHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BloggerMood) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BloggerMood) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BloggerMood) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BloggerMood) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BloggerMood) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BloggerMood) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BloggerMood) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BloggerMood) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BloggerMood) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bloggerMoodAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBloggerMoodHook registers your hook function for all future operations.
func AddBloggerMoodHook(hookPoint boil.HookPoint, bloggerMoodHook BloggerMoodHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		bloggerMoodBeforeInsertHooks = append(bloggerMoodBeforeInsertHooks, bloggerMoodHook)
	case boil.BeforeUpdateHook:
		bloggerMoodBeforeUpdateHooks = append(bloggerMoodBeforeUpdateHooks, bloggerMoodHook)
	case boil.BeforeDeleteHook:
		bloggerMoodBeforeDeleteHooks = append(bloggerMoodBeforeDeleteHooks, bloggerMoodHook)
	case boil.BeforeUpsertHook:
		bloggerMoodBeforeUpsertHooks = append(bloggerMoodBeforeUpsertHooks, bloggerMoodHook)
	case boil.AfterInsertHook:
		bloggerMoodAfterInsertHooks = append(bloggerMoodAfterInsertHooks, bloggerMoodHook)
	case boil.AfterSelectHook:
		bloggerMoodAfterSelectHooks = append(bloggerMoodAfterSelectHooks, bloggerMoodHook)
	case boil.AfterUpdateHook:
		bloggerMoodAfterUpdateHooks = append(bloggerMoodAfterUpdateHooks, bloggerMoodHook)
	case boil.AfterDeleteHook:
		bloggerMoodAfterDeleteHooks = append(bloggerMoodAfterDeleteHooks, bloggerMoodHook)
	case boil.AfterUpsertHook:
		bloggerMoodAfterUpsertHooks = append(bloggerMoodAfterUpsertHooks, bloggerMoodHook)
	}
}

// OneP returns a single bloggerMood record from the query, and panics on error.
func (q bloggerMoodQuery) OneP() *BloggerMood {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single bloggerMood record from the query.
func (q bloggerMoodQuery) One() (*BloggerMood, error) {
	o := &BloggerMood{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for blogger_moods")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all BloggerMood records from the query, and panics on error.
func (q bloggerMoodQuery) AllP() BloggerMoodSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all BloggerMood records from the query.
func (q bloggerMoodQuery) All() (BloggerMoodSlice, error) {
	var o []*BloggerMood

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BloggerMood slice")
	}

	if len(bloggerMoodAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all BloggerMood records in the query, and panics on error.
func (q bloggerMoodQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all BloggerMood records in the query.
func (q bloggerMoodQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count blogger_moods rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q bloggerMoodQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q bloggerMoodQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if blogger_moods exists")
	}

	return count > 0, nil
}

// BloggerMoodsG retrieves all records.
func BloggerMoodsG(mods ...qm.QueryMod) bloggerMoodQuery {
	return BloggerMoods(boil.GetDB(), mods...)
}

// BloggerMoods retrieves all the records using an executor.
func BloggerMoods(exec boil.Executor, mods ...qm.QueryMod) bloggerMoodQuery {
	mods = append(mods, qm.From("`blogger_moods`"))
	return bloggerMoodQuery{NewQuery(exec, mods...)}
}

// FindBloggerMoodG retrieves a single record by ID.
func FindBloggerMoodG(id int, selectCols ...string) (*BloggerMood, error) {
	return FindBloggerMood(boil.GetDB(), id, selectCols...)
}

// FindBloggerMoodGP retrieves a single record by ID, and panics on error.
func FindBloggerMoodGP(id int, selectCols ...string) *BloggerMood {
	retobj, err := FindBloggerMood(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindBloggerMood retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBloggerMood(exec boil.Executor, id int, selectCols ...string) (*BloggerMood, error) {
	bloggerMoodObj := &BloggerMood{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `blogger_moods` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(bloggerMoodObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from blogger_moods")
	}

	return bloggerMoodObj, nil
}

// FindBloggerMoodP retrieves a single record by ID with an executor, and panics on error.
func FindBloggerMoodP(exec boil.Executor, id int, selectCols ...string) *BloggerMood {
	retobj, err := FindBloggerMood(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *BloggerMood) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *BloggerMood) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *BloggerMood) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *BloggerMood) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no blogger_moods provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bloggerMoodColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	bloggerMoodInsertCacheMut.RLock()
	cache, cached := bloggerMoodInsertCache[key]
	bloggerMoodInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			bloggerMoodColumns,
			bloggerMoodColumnsWithDefault,
			bloggerMoodColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(bloggerMoodType, bloggerMoodMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(bloggerMoodType, bloggerMoodMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `blogger_moods` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `blogger_moods` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `blogger_moods` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, bloggerMoodPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into blogger_moods")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == bloggerMoodMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for blogger_moods")
	}

CacheNoHooks:
	if !cached {
		bloggerMoodInsertCacheMut.Lock()
		bloggerMoodInsertCache[key] = cache
		bloggerMoodInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single BloggerMood record. See Update for
// whitelist behavior description.
func (o *BloggerMood) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single BloggerMood record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *BloggerMood) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the BloggerMood, and panics on error.
// See Update for whitelist behavior description.
func (o *BloggerMood) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the BloggerMood.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *BloggerMood) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	bloggerMoodUpdateCacheMut.RLock()
	cache, cached := bloggerMoodUpdateCache[key]
	bloggerMoodUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			bloggerMoodColumns,
			bloggerMoodPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update blogger_moods, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `blogger_moods` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, bloggerMoodPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(bloggerMoodType, bloggerMoodMapping, append(wl, bloggerMoodPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update blogger_moods row")
	}

	if !cached {
		bloggerMoodUpdateCacheMut.Lock()
		bloggerMoodUpdateCache[key] = cache
		bloggerMoodUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q bloggerMoodQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q bloggerMoodQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for blogger_moods")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o BloggerMoodSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o BloggerMoodSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o BloggerMoodSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BloggerMoodSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bloggerMoodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `blogger_moods` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bloggerMoodPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in bloggerMood slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *BloggerMood) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *BloggerMood) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *BloggerMood) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *BloggerMood) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no blogger_moods provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bloggerMoodColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	bloggerMoodUpsertCacheMut.RLock()
	cache, cached := bloggerMoodUpsertCache[key]
	bloggerMoodUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			bloggerMoodColumns,
			bloggerMoodColumnsWithDefault,
			bloggerMoodColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			bloggerMoodColumns,
			bloggerMoodPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert blogger_moods, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "blogger_moods", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `blogger_moods` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(bloggerMoodType, bloggerMoodMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(bloggerMoodType, bloggerMoodMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for blogger_moods")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == bloggerMoodMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for blogger_moods")
	}

CacheNoHooks:
	if !cached {
		bloggerMoodUpsertCacheMut.Lock()
		bloggerMoodUpsertCache[key] = cache
		bloggerMoodUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single BloggerMood record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *BloggerMood) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single BloggerMood record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *BloggerMood) DeleteG() error {
	if o == nil {
		return errors.New("models: no BloggerMood provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single BloggerMood record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *BloggerMood) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single BloggerMood record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BloggerMood) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no BloggerMood provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), bloggerMoodPrimaryKeyMapping)
	sql := "DELETE FROM `blogger_moods` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from blogger_moods")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q bloggerMoodQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q bloggerMoodQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no bloggerMoodQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from blogger_moods")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o BloggerMoodSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o BloggerMoodSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no BloggerMood slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o BloggerMoodSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BloggerMoodSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no BloggerMood slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(bloggerMoodBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bloggerMoodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `blogger_moods` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bloggerMoodPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from bloggerMood slice")
	}

	if len(bloggerMoodAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *BloggerMood) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *BloggerMood) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *BloggerMood) ReloadG() error {
	if o == nil {
		return errors.New("models: no BloggerMood provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BloggerMood) Reload(exec boil.Executor) error {
	ret, err := FindBloggerMood(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *BloggerMoodSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *BloggerMoodSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BloggerMoodSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty BloggerMoodSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BloggerMoodSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	bloggerMoods := BloggerMoodSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bloggerMoodPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `blogger_moods`.* FROM `blogger_moods` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, bloggerMoodPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&bloggerMoods)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BloggerMoodSlice")
	}

	*o = bloggerMoods

	return nil
}

// BloggerMoodExists checks if the BloggerMood row exists.
func BloggerMoodExists(exec boil.Executor, id int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `blogger_moods` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if blogger_moods exists")
	}

	return exists, nil
}

// BloggerMoodExistsG checks if the BloggerMood row exists.
func BloggerMoodExistsG(id int) (bool, error) {
	return BloggerMoodExists(boil.GetDB(), id)
}

// BloggerMoodExistsGP checks if the BloggerMood row exists. Panics on error.
func BloggerMoodExistsGP(id int) bool {
	e, err := BloggerMoodExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// BloggerMoodExistsP checks if the BloggerMood row exists. Panics on error.
func BloggerMoodExistsP(exec boil.Executor, id int) bool {
	e, err := BloggerMoodExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
